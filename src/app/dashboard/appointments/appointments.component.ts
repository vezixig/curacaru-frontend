import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'cura-appointments',
  imports: [RouterModule],
  standalone: true,
  templateUrl: 'appointments.component.html',
})
export class AppointmentsComponent {}
